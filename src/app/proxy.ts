export interface Proxy  {
  ip: string;
  port: number;
  code: string;
  https: string;
  provider: string;
  basicFunctionality: boolean;
  testDate: string;
  createdAt: Date;
  updatedAt: Date;
}
